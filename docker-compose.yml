version: "3"
services:
       speedtester:
           container_name: speedtester       
           restart: always
           build: .
           ports:
             - "8080:8080"
           external_links:
             - mongo
       mongo:
             container_name: mongo
             image: mongo
             volumes:
               - ./data:/data/db
             ports:
               - '27018:27017'